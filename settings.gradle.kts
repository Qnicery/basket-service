rootProject.name = "basket"

pluginManagement {
    repositories {
        gradlePluginPortal() // основной репозиторий плагинов Gradle
        mavenCentral()       // дополнительный репозиторий для Kotlin плагинов
    }
}
